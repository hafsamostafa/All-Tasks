/* Assignments
=============
Compute area and circumference of an circle [pi * Math.pow(r , 2)] */

let r    = parseFloat (prompt ("Please enter your circule Radious to calculate the area"));
let Area = [Math.PI*Math.pow (2, r)];
console.log (`Your circul are equals ${Area}`)

